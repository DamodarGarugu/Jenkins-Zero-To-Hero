pipeline {
  agent {
    docker { image 'openjdk:11' }
  }
  stages {
    stage('Test_Java_Ver') {
      steps {
        def javaHome = tool 'JDK11'
        sh 'java --version'
        sh 'javac my-first-pipeline/Test.java'
        sh '${javaHome}/bin/java -cp build Test'

      }
      }
    }
  }
